$("btn-info").on('submit', handleFormSubmit);

$(function () {
    $('#btn btn-info').submit();
  });

  $("#displayform").hide(); 

  addEventListener(Click.btn btn-info)
  $('#projectModal').dialogue;
  $('#projectModal').modal('hide');